# 《锻铸与咒印》落地实施手册
> **工具链：Codex + GPT 5.3 编码 ｜ Nano Banana API 出图**

---

## 先解决最大的坑：Nano Banana 出图尺寸不可控

### 策略：CSS 端统一裁切，不依赖原图尺寸

给 Codex 的全局规则（写进项目的 `AGENTS.md` 或 `codex-instructions.md`）：

```markdown
## 图片处理规则
- 所有图片素材尺寸不可预测，必须用 CSS 容器控制显示尺寸
- 图片容器必须设定固定宽高，图片用 object-fit: cover 或 contain
- 角色/敌人立绘用 object-fit: contain（不裁切）
- 纹理背景用 background-size: cover 或 512px 512px repeat
- 卡牌插画用 object-fit: cover + object-position: center
- 所有图片加 loading="lazy"
```

对应的基础 CSS：

```css
/* 图片容器通用方案 */
.img-cover {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center;
}
.img-contain {
  width: 100%;
  height: 100%;
  object-fit: contain;
  object-position: center bottom;
}
```

---

## 整体分成 5 个批次，每批次给你：
1. **给 Codex 的任务 prompt**（直接复制）
2. **给 Nano Banana 的出图 prompt**（直接复制）
3. **验收标准**（你自己看着检查）

---

---

## 批次 1：地基层（Design Tokens + 全局样式 + 3 个基础组件）

### Task 1.1 — 给 Codex 的 prompt

```
创建全局设计 Token 文件和基础样式。

1. 创建 src/ui/styles/tokens.css，写入以下 CSS 变量：

:root {
  /* Paper 纸张底色 */
  --paper-base: #F5E6C8;
  --paper-warm: #EDD9B5;
  --paper-cool: #E8E0D0;
  --paper-aged: #D9C9A3;
  --paper-dark: #C4AE82;
  --paper-cream: #FAF4E8;

  /* 功能色 */
  --color-hp: #B84233;
  --color-hp-light: #D4736A;
  --color-mana: #3B5E8C;
  --color-mana-light: #6A8FBD;
  --color-heal: #5B8C5A;
  --color-heal-light: #8FBD8E;
  --color-gold: #C8973E;
  --color-gold-light: #E8C36A;
  --color-danger: #9E2B1C;
  --color-curse: #6B3A6B;

  /* 流派色 */
  --arch-sword-primary: #A86E3E;
  --arch-sword-secondary: #787878;
  --arch-staff-primary: #3A6B7C;
  --arch-staff-secondary: #6B4F7A;

  /* 文字 */
  --ink-primary: #3A2E1E;
  --ink-secondary: #6B5D4A;
  --ink-tertiary: #9E8E78;
  --ink-inverse: #FAF4E8;

  /* 阴影 */
  --shadow-s: 1px 2px 2px rgba(58,46,30,0.12);
  --shadow-m: 2px 3px 4px rgba(58,46,30,0.15);
  --shadow-l: 3px 5px 8px rgba(58,46,30,0.20);
  --shadow-xl: 5px 8px 16px rgba(58,46,30,0.25);

  /* 间距 */
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 24px;
  --space-6: 32px;
  --space-7: 48px;

  /* 缓动 */
  --ease-paper: cubic-bezier(0.25, 0.46, 0.45, 0.94);
  --ease-bounce: cubic-bezier(0.34, 1.56, 0.64, 1);
  --ease-smooth: cubic-bezier(0.4, 0, 0.2, 1);
}

2. 创建 src/ui/styles/global.css：
- 引入 Google Fonts: ZCOOL KuaiLe, Fredericka the Great, Fredoka One, Noto Sans SC (300,400)
- body 设置: background-color: var(--paper-base), color: var(--ink-primary), font-family: "Noto Sans SC"
- body 加 background-image 引用纹理图（路径 assets/textures/tex-kraft-base.png），
  background-size: 512px 512px, background-blend-mode: multiply
- 设定 CSS 变量给字体族: 
  --font-display: "ZCOOL KuaiLe", cursive
  --font-display-en: "Fredericka the Great", cursive
  --font-number: "Fredoka One", sans-serif
  --font-body: "Noto Sans SC", sans-serif
- 全局 box-sizing: border-box
- 游戏容器 #app: width 1280px, height 720px, margin auto, overflow hidden, position relative

3. 确保 tokens.css 在 global.css 之前导入。
```

### Task 1.2 — 给 Codex 的 prompt（按钮组件）

```
创建按钮组件样式 src/ui/styles/components/button.css

设计规范：
- 这是一个多层立体剪纸拼贴画风格的游戏
- 所有按钮看起来像"被剪出来贴上去的纸片"

.btn 基础样式：
- border: 2px solid #FFFFFF（白色裁纸边）
- border-radius: 3px（手工裁切微圆角，不要大圆角）
- box-shadow: var(--shadow-m)
- background: var(--paper-cream)
- color: var(--ink-primary)
- font-family: var(--font-display)
- cursor: pointer
- transition: all 150ms var(--ease-smooth)
- 文字带微弱 text-shadow: 0 1px 0 rgba(255,255,255,0.4)

.btn:hover → transform: translateY(-2px), box-shadow: var(--shadow-l)
.btn:active → transform: translateY(1px), box-shadow: var(--shadow-s)
.btn:disabled → opacity 0.5, box-shadow none, pointer-events none

尺寸变体：
.btn-lg → height 48px, padding 12px 32px, font-size 18px
.btn-md → height 36px, padding 8px 24px, font-size 15px
.btn-sm → height 28px, padding 4px 16px, font-size 13px

颜色变体：
.btn-primary → background: var(--color-gold), 略深的 hover 态
.btn-danger → background: var(--color-hp), color: #fff
.btn-ghost → background: transparent, border: 2px dashed var(--ink-tertiary)
```

### Task 1.3 — 给 Codex 的 prompt（面板组件）

```
创建面板组件样式 src/ui/styles/components/panel.css

.panel 基础样式：
- background: var(--paper-cream)
- border: 3px solid #FFFFFF
- border-radius: 3px
- box-shadow: var(--shadow-l)
- padding: var(--space-4)

.panel-title：
- font-family: var(--font-display)
- font-size: 20px
- color: var(--ink-primary)
- padding-bottom: var(--space-2)
- border-bottom: 2px dashed var(--ink-tertiary)（缝线效果）
- margin-bottom: var(--space-3)

.panel-body：
- font-family: var(--font-body)
- font-size: 14px
- line-height: 1.5

模态弹窗版：
.modal-overlay：
- position fixed 全屏
- background: rgba(58,46,30,0.4)
- backdrop-filter: blur(2px)
- display flex 居中

.modal-overlay .panel：
- box-shadow: var(--shadow-xl)
- 入场动画: scale(0.8) opacity(0) → scale(1) opacity(1), 300ms ease-smooth
```

### Task 1.4 — 给 Codex 的 prompt（卡牌组件）

```
创建卡牌组件 src/ui/styles/components/card.css
以及对应的 HTML 结构模板。

卡牌比例 2.5:3.5。

三套尺寸：
- 手牌: 100×140px
- 展示: 140×196px（奖励/商店）
- 预览: 180×252px（hover放大）

.card 结构（从上到下）：
1. 费用角标 .card-cost：
   - 左上角绝对定位
   - 用 clip-path: polygon(0 0, 100% 0, 0 100%) 做三角形
   - 宽高 28px
   - 背景 var(--color-mana)
   - 白色数字，font-family: var(--font-number)，居中偏左上

2. 插画区 .card-art：
   - 占卡牌高度 45%
   - overflow hidden
   - 内部 img 用 object-fit: cover（因为素材尺寸不可控）
   - 带 1px solid var(--ink-tertiary) 内边框

3. 缝线分隔 .card-divider：
   - height 0, border-top: 1.5px dashed var(--ink-tertiary)
   - margin: 4px 0

4. 卡牌名 .card-name：
   - font-family: var(--font-display), 14px, 居中
   
5. 类型标签 .card-type：
   - font-size 10px, color var(--ink-secondary), 居中

6. 描述区 .card-desc：
   - font-family: var(--font-body), 11px, 居中
   - padding 0 6px
   - 描述中的数字用 span.card-value，color var(--color-gold), font-family var(--font-number)

7. 升级标记 .card-upgraded：
   - 右下角绝对定位, "★" 字符, color var(--color-gold), font-size 14px

外框：
- border: 2px solid #FFFFFF
- border-radius: 3px
- box-shadow: var(--shadow-m)
- overflow hidden

卡牌类型变体（底色区分）：
.card--attack → background: #F2E0CC
.card--defend → background: #E8EDDA
.card--skill  → background: #DDE6EE
.card--power  → background: #F0EADB
.card--curse  → background: #E8DCE8

手牌 hover 效果：
.hand-area .card:hover → 
  transform: translateY(-30px) scale(1.4)
  z-index: 100
  box-shadow: var(--shadow-xl)
  transition: 200ms var(--ease-smooth)
```

### 给 Nano Banana 的出图 prompt — 纹理

> 逐个调用，获取 6 张纹理

```
Prompt 1 (tex-kraft-base):
seamless tileable kraft paper texture, natural brown paper, 
visible paper fibers, slightly yellowish warm tone, 
subtle grain, clean, no text, no objects, 
flat top-down view, soft even lighting

Prompt 2 (tex-kraft-warm):
seamless tileable kraft paper texture, warm orange-tinted 
brown paper, cozy warm tone like firelight on paper, 
subtle grain, no text, no objects, flat top-down view

Prompt 3 (tex-kraft-cool):
seamless tileable kraft paper texture, cool grey-blue tinted 
parchment, subtle grain, slightly desaturated, 
no text, no objects, flat top-down view

Prompt 4 (tex-kraft-dark):
seamless tileable aged dark brown paper texture, 
weathered old paper with visible creases and stains, 
vintage distressed look, no text, no objects, flat top-down view

Prompt 5 (tex-kraft-aged):
seamless tileable ancient parchment texture, 
old yellowed edges, coffee stain marks, worn look, 
antique map paper feel, no text, no objects, flat top-down view

Prompt 6 (tex-washi):
seamless tileable japanese washi paper texture, 
soft white cream color, visible delicate fibers, 
translucent quality, clean minimal, no text, flat top-down view
```

**拿到图后处理**：不管图多大，CSS 里用 `background-size: 512px 512px` 平铺即可。

### 批次 1 验收标准

```
□ 打开游戏，背景是牛皮纸色+纸张纹理
□ 标题文字是手写涂鸦风格字体
□ 数字是圆润的 Fredoka One 字体
□ 按钮有白色边、hover 浮起、按下回弹
□ 弹窗有半透明遮罩和缝线分隔
□ 卡牌有费用角标、名称、描述、类型底色区分
```

---

---

## 批次 2：四个核心场景骨架（Title + Battle + Map + Reward）

### Task 2.1 — Title 场景 — 给 Codex 的 prompt

```
重构 src/ui/scenes/title.ts 的样式和布局。

设计基准: 1280×720px

布局结构（垂直居中，自上而下）：
- 整个场景分3层（用 z-index 区分）：
  - 底层 .title-bg：全屏牛皮纸 + 森林背景图 img-contain
  - 中层 .title-content：标题文字 + 按钮
  - 顶层 .title-leaves：装饰性飘落树叶（后期再加，先留空 div）

.title-content 布局：
- display flex, flex-direction column, align-items center, justify-content center
- 高度 100%

标题区：
- h1 "锻铸与咒印"
  font-family: var(--font-display)
  font-size: 52px
  color: var(--ink-primary)
  text-shadow: 0 2px 4px rgba(58,46,30,0.3), 0 0 0 3px #fff（白描边模拟）
  用 -webkit-text-stroke: 2px #fff 做白描边更好
  letter-spacing: 0.1em
  
- p.subtitle "Forge & Glyph"
  font-family: var(--font-display-en)
  font-size: 26px
  color: var(--ink-secondary)
  letter-spacing: 0.15em
  margin-top: var(--space-2)

按钮区（margin-top: var(--space-7)）：
- #btn-start: class="btn btn-primary btn-lg"，文字"⚔️ 开始冒险"
  width: 240px
- #btn-style-lab: class="btn btn-md"，文字"🎨 样式工坊"
  width: 200px, margin-top: var(--space-3)

背景图 .title-bg：
- position absolute, inset 0
- 内含一张森林背景图片 <img src="assets/scenes/title-forest.png">
- img 用 object-fit: cover, width 100%, height 100%, opacity 0.4
- 这样背景图不管多大都能铺满，半透明叠在牛皮纸上

简单入场动画（CSS animation）：
- 标题: fadeInDown 0.6s ease-out（从上方淡入下移）
- 按钮: fadeInUp 0.4s ease-out, delay 0.4s（从下方淡入上移）
- 定义 @keyframes fadeInDown 和 fadeInUp
```

### Task 2.2 — Battle 场景 — 给 Codex 的 prompt

```
重构 src/ui/scenes/battle.ts 的样式和布局。

设计基准: 1280×720px，横版布局。

整体用 CSS Grid 或 Flex 分区：

┌──────────────────────────────────────────────┐
│            .player-bar (顶部状态栏)            │  height: 52px
├────────────────────┬─────────────────────────┤
│  .player-area      │      .enemy-area        │  flex: 1
│  (左侧 35%)        │      (右侧 65%)          │  (约 400px 高)
├────────────────────┴─────────────────────────┤
│         .battle-material-bar                  │  height: 44px
├──────────────────────────────────────────────┤
│              .hand-area                       │  height: 172px
│         (手牌 + 普攻/结束回合按钮)              │
└──────────────────────────────────────────────┘

各区域样式：

1. .player-bar（顶部）：
   - background: var(--paper-cream), border-bottom: 2px dashed var(--ink-tertiary)
   - 水平排列: 血量条 | 护甲 | 能量 | 金币 | 状态说明按钮
   - 血量条 .hp-bar：
     width: 200px, height: 20px
     外框: border 2px solid #fff, border-radius 3px, background var(--paper-dark)
     填充: .hp-bar-fill, background linear-gradient(90deg, var(--color-hp), var(--color-hp-light))
     transition: width 0.4s ease
     数值文字覆盖在条上方: 绝对定位, font-family var(--font-number), 14px, 白色
   - 护甲: 🛡️ + 数字
   - 能量: ◆ + 数字, color var(--color-mana)
   - 金币: 💰 + 数字, color var(--color-gold)

2. .player-area（左侧）：
   - display flex, flex-direction column, align-items center, justify-content center
   - 玩家角色图: .player-sprite
     宽 160px, 高 240px 的容器, 内部 img 用 object-fit: contain
     边框: 不加（角色纸偶自带白边，在图片里）
   - 角色下方: Buff 图标行 .buff-row
     水平排列 20×20 的小方块

3. .enemy-area（右侧）：
   - display flex, flex-wrap wrap, justify-content center, align-items center, gap 16px
   - 每个敌人 .enemy-slot：
     width: 140px（1-2个敌人时可更大）
     display flex, flex-direction column, align-items center
   - 敌人图 .enemy-sprite: 120×160 容器, img object-fit contain
   - 敌人意图 .enemy-intent: 
     在敌人图上方, 小面板样式
     background var(--paper-cream), border 1.5px solid #fff, border-radius 3px
     padding 2px 8px, font-size 13px, box-shadow var(--shadow-s)
   - 敌人血条: 同 hp-bar 但更窄, width 100px, height 8px

4. .battle-material-bar（中部横条）：
   - background: var(--paper-aged), border-top/bottom: 1.5px dashed var(--ink-tertiary)
   - 水平排列材料图标（32×32 容器 + 数量文字）
   - padding: 6px 16px, gap 12px

5. .hand-area（底部）：
   - height: 172px, position relative
   - 卡牌容器 .hand-cards: 
     display flex, justify-content center, align-items flex-end
     height: 140px（卡牌高度）
     padding-bottom: 8px
   
   扇形排列用 CSS 实现（每张卡通过内联 style 设置）：
   每张卡牌: position relative, transition all 200ms var(--ease-smooth)
   transform: rotate(Xdeg) translateY(Ypx)
   其中：
     totalSpread = Math.min(cardCount * 8, 60) 度
     每张卡的 rotation = (index - (count-1)/2) * (totalSpread / Math.max(count-1, 1))
     每张卡的 Y偏移 = Math.abs(index - (count-1)/2) * 6（中间低两侧高，形成弧线）
     margin-left: -20px（卡牌重叠）, 第一张除外
   
   扇形的 JS 计算放在 battle.ts 的渲染逻辑里。

   按钮区: position absolute, right 16px, bottom 8px
   #btn-normal-attack: class="btn btn-md", "⚔️ 普攻"
   #btn-end-turn: class="btn btn-md btn-ghost", "⏭️ 结束回合"
   两按钮竖排, gap 8px

战斗场景背景:
- body 在战斗时 background 纹理切换为 tex-kraft-dark
  或在 .scene-battle 上单独覆盖 background
```

### Task 2.3 — Map 场景 — 给 Codex 的 prompt

```
重构 src/ui/scenes/map.ts 的样式和布局。

布局：
┌──────────────────────────────────────────────┐
│  .map-player-bar (同 battle 的 player-bar)    │  52px
├──────────────────────────────────────────────┤
│  #map-scroll                                  │  flex: 1
│  (纵向滚动的地图区)                             │  overflow-y: auto
│                                                │
│    .map-canvas (实际地图内容)                    │
│      svg.map-links (路径层)                     │
│      .map-node × N (节点层)                     │
│                                                │
└──────────────────────────────────────────────┘

地图背景：
- #map-scroll: background-image url(tex-kraft-aged.png), background-size 512px 512px
- 添加纸张边缘做旧效果: 
  box-shadow: inset 0 0 60px rgba(58,46,30,0.3)（内阴影模拟旧纸边缘焦黄）

地图节点 .map-node：
- width: 44px, height: 44px
- border-radius: 50%（默认圆形）
- border: 2px solid #fff
- box-shadow: var(--shadow-m)
- display flex, align-items center, justify-content center
- font-size: 20px（用 emoji 作为图标）
- cursor pointer
- transition: all 200ms var(--ease-smooth)
- position absolute（通过 left/top 定位在 .map-canvas 上）

节点类型对应：
.map-node--battle    → background: #E8D0CC, 内容 "⚔️"
.map-node--elite     → background: #D4B8B0, border-color: var(--color-gold), 内容 "💀"
.map-node--boss      → background: #C4A8A0, border-color: var(--color-gold), width/height 56px, 内容 "👑"
.map-node--campfire  → background: #F0DCC0, 内容 "🔥"
.map-node--shop      → background: #E8DCC0, 内容 "🛒"
.map-node--event     → background: #D8DDE8, 内容 "❓"
.map-node--forge     → background: #D8D0C0, 内容 "⚒️"
.map-node--enchant   → background: #D0D4E8, 内容 "🔮"
.map-node--treasure  → background: #E8DCA0, 内容 "💎"
.map-node--temple    → background: #E8E0D0, 内容 "🏛️"
.map-node--trial     → background: #D8C8C0, 内容 "⚡"

节点交互：
.map-node--available → 添加 animation: breathe 2s ease-in-out infinite
  @keyframes breathe { 0%,100% { transform: scale(1) } 50% { transform: scale(1.08) } }
.map-node--available:hover → transform scale(1.15), box-shadow var(--shadow-l)
.map-node--completed → opacity 0.5, 加 "✓" 伪元素 ::after
.map-node--current → box-shadow: 0 0 0 3px var(--color-gold), var(--shadow-l)

路径 SVG .map-links：
- 与 .map-canvas 同尺寸, position absolute, top 0, left 0
- 每条路径用 <path> 元素
- 路径样式:
  stroke-dasharray: 8 5（针脚缝线效果）
  stroke-width: 2.5
  stroke-linecap: round
  fill: none
- .map-link--available → stroke: var(--ink-secondary)
- .map-link--completed → stroke: var(--ink-primary), stroke-dasharray: none（实线）
- .map-link--locked → stroke: var(--ink-tertiary), opacity 0.3

路径从直线改曲线：
目前路径是直线连接两点。改为二次贝塞尔曲线：
计算两点中点，向左或右偏移 20-40px 作为控制点，
用 SVG `M x1,y1 Q cx,cy x2,y2` 命令绘制。
偏移方向交替（避免所有曲线同向弯曲）。
```

### Task 2.4 — Reward 场景 — 给 Codex 的 prompt

```
重构 src/ui/scenes/reward.ts 的样式和布局。

布局（居中面板）：
- 全屏 overlay background rgba(58,46,30,0.3)
- 居中 .reward-panel：
  width: 800px, max-height: 600px
  用 .panel 样式

结构：
1. 标题 "🎉 战斗胜利"，font-family var(--font-display), 28px, 居中
2. 缝线分隔
3. 卡牌奖励区 .reward-cards：
   - 横向排列 3 张卡牌（用 .card 展示态 140×196）
   - gap: 24px, justify-content center
   - 每张卡可点击，hover 浮起放大
   - 选中后: 该卡 border-color var(--color-gold), 其他两张 opacity 0.4
4. 缝线分隔
5. 材料/武器掉落区：
   - 横排小面板显示材料图标和武器
6. 跳过按钮 #btn-skip：
   - class="btn btn-ghost btn-md", 文字 "跳过 → 获得 25💰"
   - 底部居中

入场动画：
- 面板 scale(0.85) opacity(0) → scale(1) opacity(1), 300ms
- 3张卡牌依次出现: 每张 delay 100ms, translateY(20px) opacity(0) → normal
```

### 给 Nano Banana 的出图 prompt — 标题页背景

```
Prompt (title-forest):
layered paper cut art, enchanted forest scene, 
multiple layers of paper trees creating depth, 
brown kraft paper silhouettes, white cut paper edges visible, 
earth tones, low saturation vintage colors, 
fairy tale atmosphere, children's storybook illustration, 
whimsical, warm golden light from top left, 
paper craft art style, handmade feel, 8K detailed
```

### 批次 2 验收标准

```
□ Title: 标题居中，有手写风格字体，两个按钮可点击，背景有森林图
□ Battle: 四区划分清晰（顶栏/角色区/敌人区/手牌区）
□ Battle: 血条有颜色填充和数值
□ Battle: 手牌呈扇形排列，hover 某张牌会浮起
□ Map: 节点用 emoji 区分类型，可点击的节点有呼吸动画
□ Map: 路径是虚线（针脚线），曲线连接
□ Reward: 弹出面板，3张卡横排，可点选
```

---

---

## 批次 3：剩余 10 个场景套模板

### 核心思路

> 这 10 个场景结构高度相似：**标题 + 面板 + 按钮/选项**。
> 给 Codex 一个统一的 prompt 模板，换内容即可。

### 给 Codex 的统一场景 prompt 模板

```
重构 src/ui/scenes/[场景名].ts 的样式和布局。

使用项目已有的组件样式：
- 按钮: .btn .btn-primary .btn-md 等（见 button.css）
- 面板: .panel .panel-title（见 panel.css）
- 卡牌: .card .card--attack 等（见 card.css）
- 全局色彩和间距: var(--xxx)（见 tokens.css）

场景背景纹理: [指定纹理]
布局: [具体布局描述]
元素: [具体元素描述]

通用规则：
- 所有分隔线用 border-top: 2px dashed var(--ink-tertiary)
- 所有标题用 font-family: var(--font-display)
- 所有图片容器设定固定宽高，img 用 object-fit contain 或 cover
- 场景入场统一: opacity 0 → 1, 300ms
```

### 各场景的具体填充内容

**给 Codex 的 prompt — 一次发一个场景：**

#### Style Lab

```
场景: style-lab（样式测试工坊）
背景纹理: tex-kraft-base + 叠加浅灰色网格线（用 CSS repeating-linear-gradient 画 40px 方格）
布局: 顶部横条（返回按钮 + 标题） → Tabs 横排 → 预览区（带虚线边框）→ 底部说明区
元素:
- #btn-style-lab-back: class="btn btn-ghost btn-sm", 文字 "← 返回首页"
- Tabs [data-preview-mode]: 水平排列的标签按钮，选中态底色 var(--paper-cream) 无下边框，未选中态底色 var(--paper-aged)
- #style-lab-preview: width 100%, flex 1, border: 2px dashed var(--ink-tertiary), overflow hidden
```

#### Weapon Select

```
场景: weapon-select（起始武器选择）
背景纹理: tex-kraft-warm
布局: 标题居中顶部 → 两张大卡片左右对称排列
元素:
- 标题 "选择你的起始武器", font-display, 32px
- 副标题 "每位工匠都有自己的道路", font-body, color var(--ink-secondary), 14px
- 两张武器面板, 各 260×400px, 用 .panel 样式
  内含: 武器图片区(160×200 容器) + 武器名 + 属性列表 + 描述文字 + 选择按钮
- #btn-pick-longsword: class="btn btn-primary btn-md", 背景加 var(--arch-sword-primary)
- #btn-pick-staff: class="btn btn-primary btn-md", 背景加 var(--arch-staff-primary)
- hover 武器面板: translateY(-8px), shadow-l
```

#### Campfire

```
场景: campfire（篝火休憩）
背景纹理: tex-kraft-warm
布局: 标题 → 篝火图片区(居中，240×200 容器) → 三选一横排面板
元素:
- 标题 "🔥 篝火休憩", 副标题 "在温暖中恢复力量…"
- 篝火图: 居中 img contain
- 三个选项面板, 各 220×180px, 用 .panel:
  1. "❤️ 休息" — 恢复30%HP
  2. "⬆️ 升级卡牌" — 选择一张卡牌升级
  3. "➡️ 继续旅程" — 返回地图
- 每个面板底部一个 .btn .btn-md
- hover 面板: translateY(-6px)
- 只能选一项：选中后其他灰化
```

#### Shop

```
场景: shop（商店）
背景纹理: tex-cardboard
布局: 顶部（招牌标题 + 余额） → Tabs（卡牌/材料/服务） → 商品网格 → 底部离开按钮
元素:
- 标题 "🏪 旅途商店", 右侧 "💰 余额: {N}"
- Tabs 三个: 🃏卡牌 | 📦材料 | 🔧服务
- 卡牌Tab: 横排最多5张卡（.card 展示态），每张下方价格标签 "💰{N}"
- 材料Tab: 横排材料图标 32×32 + 价格
- 服务Tab: 3个横条面板（治疗/删卡/变卡），每个有描述和价格
- 买不起的: opacity 0.5, 价格变 color var(--color-hp)
- 已购买: 覆盖 "已售罄" 文字，opacity 0.3
- #btn-shop-leave: class="btn btn-md", "🚪 离开商店", 底部居中
```

#### Inventory

```
场景: inventory（背包）
背景纹理: tex-linen（浅亚麻色）
布局: 全屏面板覆盖（modal 样式）→ 顶部标题+关闭 → Tabs → 左侧列表+右侧详情 → 底部统计
元素:
- 标题 "📌 背包", 右上 #btn-close-inventory class="btn btn-ghost btn-sm" "✕"
- Tabs: ⚔️武器 | 🃏卡组 | 📦材料
- 武器Tab: 左侧武器列表（每项 60×80 缩略卡 + 装备按钮 [data-weapon-uid]），当前装备金色边框；右侧武器详情面板
- 卡组Tab: 网格展示全部卡牌（80×112 缩略），可滚动（自定义滚动条样式）
- 材料Tab: 图标网格 48×48
- 底部: 卡组统计 "总计 N张 | 攻击N | 防御N | 技能N | 能力N"，用小标签贴色块
```

#### Forge

```
场景: forge（工坊）
背景纹理: tex-kraft-warm + tex-cardboard 叠加
布局: 标题 → 四选一大面板横排 → 底部当前材料展示
元素:
- 标题 "⚒️ 铁匠工坊", 副标题 "选择一项工艺，仅此一次"
- 四个面板 240×300px:
  🔨锻造（消耗材料造装备）| ✨附魔 | ⬆️升级卡牌 | ✂️精简卡组
  每个面板: 图标 + 名称 + 描述 + 材料需求 + 选择按钮
  材料不足: 面板 opacity 0.5, 需求数字 color var(--color-hp)
- 底部材料栏: "📦 当前材料: [图标×N] [图标×N]..."
- 选择后弹出操作 modal
```

#### Enchant

```
场景: enchant（附魔台）
背景纹理: tex-kraft-cool
布局: 标题 → 左侧当前武器面板 + 右侧可用附魔列表 → 底部精华和返回按钮
元素:
- 标题 "🔮 附魔台"
- 左面板(400×400): 武器图(200×280容器) + 武器名 + 附魔槽位(3个圆形 28×28)
  槽位: 有附魔=填色圆+图标, 空=虚线圆圈"+"
  火焰/寒冰槽位: background 对应色 + 渐变模拟宝石光泽
  其他槽位: 用方形纸片样式，旋转 ±5deg 模拟贴歪的符印贴纸
- 右面板(scrollable): 附魔选项列表
  每项 [data-enchant-id]: 横条面板, 图标+名称+效果描述+所需精华
- 底部: 精华库存 + "← 返回地图" 按钮
```

#### Event

```
场景: event（随机事件）
背景纹理: 根据数据中的事件类型切换 tex-kraft-warm/cool/dark
布局: 居中大面板 700×550
元素:
- 事件插画区: 顶部 660×220 容器, img object-fit cover, border-radius 3px
- 事件标题: font-display 24px
- 缝线分隔
- 叙事文字: font-family var(--font-flavor)("FZQKBYSJW" 或 "Noto Serif SC"), 14px, line-height 1.8, color var(--ink-secondary)
- 选项区: 竖排按钮列表 .btn-event [data-option-id]
  每个选项: 全宽横条, 左对齐文字, border 2px solid #fff, padding 12px 16px
  hover: translateX(4px), 左侧出现 "►", background 轻微变亮
  已选: border-color var(--color-gold)
```

#### Act Transition

```
场景: act-transition（幕间过渡）
背景纹理: tex-kraft-cool
按 mode 显示不同内容:

共用: 标题 "📖 第 X 幕结束", 副标题 "新的篇章即将展开…"

mode=none（选增益）: 
  三个面板横排 (同篝火样式)，每个是一项增益
  
mode=knowledge_pick / foresight_pick（选卡）:
  横排展示 5 张卡牌 (.card 展示态)
  底部提示 "选择 N 张加入卡组" + 确认按钮

mode=knowledge_remove / deep_purify（删卡）:
  网格展示全部卡牌（同背包卡组Tab），可多选
  选中的卡牌: border-color var(--color-hp), 加 "✂️" 角标
  底部: "选择 N 张移除" + 确认按钮 class="btn btn-danger"
```

### 批次 3 验收标准

```
□ 全部 14 个场景都能正常显示和交互
□ 统一使用 .btn / .panel / .card 组件样式
□ 每个场景有正确的背景纹理
□ 标题统一使用手写字体
□ 分隔线统一使用 dashed 缝线样式
```

---

---

## 批次 4：出图+替换占位素材

### 你需要出的图（按优先级排序）

| 优先级 | 素材 | 数量 | Nano Banana prompt |
|---|---|---|---|
| P0 | 标题页森林背景 | 1 张 | 见下方 |
| P0 | 玩家纸偶（持剑） | 1 张 | 见下方 |
| P0 | 玩家纸偶（持杖） | 1 张 | 见下方 |
| P0 | 基础敌人 3-5 个 | 3-5 张 | 见下方 |
| P1 | 武器展示图 | 2 张 | 见下方 |
| P1 | 篝火 | 1 张 | 见下方 |
| P2 | 卡牌插画 | N 张 | 见下方模板 |
| P2 | 事件场景图 | N 张 | 见下方模板 |
| P3 | 装饰元素（纸胶带等） | 5-8 张 | 见下方 |

### Nano Banana prompt 合集

**通用前缀（每个 prompt 都加这段）：**

```
paper craft cutout art style, white cut paper edges, 
cartoon ink outline, kraft paper texture, 
low saturation earth tone vintage colors, 
children's storybook illustration, whimsical fairy tale, 
handmade paper art feel, layered paper depth,
```

**然后接具体内容：**

```
【玩家-剑士】
[通用前缀]
a young adventurer character paper doll, holding a bronze sword, 
standing pose facing right, full body, simple paper craft base/stand, 
warm copper and brown tones, cute proportions, 
transparent background or plain kraft paper background,
single character isolated

【玩家-法师】
[通用前缀]
a young mage character paper doll, holding a crystal staff, 
standing pose facing right, full body, paper craft base/stand,
indigo blue and purple tones, cute proportions,
transparent background or plain kraft paper background,
single character isolated

【普通敌人-森林狼】
[通用前缀]
a fierce wolf monster paper cutout, dark grey and brown tones,
aggressive pose, slightly menacing but cute, 
paper puppet style with visible paper layers,
transparent background or plain kraft paper background,
single creature isolated

【普通敌人-蘑菇怪】
[通用前缀]
a mushroom creature monster paper cutout, red spotted cap,
grumpy expression, short and round, earthy brown and red tones,
paper puppet style, single creature isolated,
transparent background or plain kraft paper background

【普通敌人-石头魔像】
[通用前缀]
a stone golem monster paper cutout, grey and brown tones,
bulky rocky body, simple geometric shapes, ancient runes on body,
paper puppet style, single creature isolated,
transparent background or plain kraft paper background

【Boss-暗影树人】
[通用前缀]
a giant dark treant boss monster paper cutout, 
twisted dark branches, glowing purple eyes, 
intimidating but paper craft style, dark brown and purple tones,
larger than normal, paper puppet style,
transparent background or plain kraft paper background

【武器-长剑】
[通用前缀]
a bronze longsword weapon paper cutout, top-down flat lay view,
warm copper color with grey steel blade, simple elegant design,
single weapon isolated, transparent background

【武器-法杖】
[通用前缀]
a crystal mage staff weapon paper cutout, top-down flat lay view,
wooden shaft with blue crystal top, indigo and brown tones,
single weapon isolated, transparent background

【篝火场景】
[通用前缀]
a cozy campfire scene, layered paper flames in orange red yellow,
stone circle around fire, small logs, warm golden light,
forest background silhouettes, peaceful nighttime atmosphere

【卡牌插画模板 — 替换方括号内容】
[通用前缀]
[卡牌内容描述，如 "a spinning slash attack with sword motion lines"],
small square illustration, simple composition,
framed within paper craft border, centered subject

【事件插画模板 — 替换方括号内容】
[通用前缀]
[事件场景描述，如 "a wounded traveler sitting by the roadside in a forest"],
wide horizontal scene illustration, storytelling composition,
layered paper cut forest background

【装饰-纸胶带】
a strip of semi-transparent washi masking tape, 
pastel green color with subtle pattern, 
slightly angled, realistic paper texture,
isolated on white background, flat lay view,
no text

【装饰-蜡封】
a red wax seal stamp on kraft paper, 
circular with crossed swords emblem,
vintage paper craft style, realistic texture,
isolated, flat lay top-down view
```

### 出图后的处理流程

> 因为尺寸不可控，每张图拿到后这样处理：

```
给 Codex 的 prompt（图片处理）:

我获得了一批游戏素材图片，尺寸各不相同。
请确保项目中所有使用这些图片的地方都遵循以下规则：

1. 角色/敌人图片 (.player-sprite img, .enemy-sprite img):
   - 容器设定固定宽高
   - img: width 100%, height 100%, object-fit: contain, object-position: center bottom

2. 卡牌插画 (.card-art img):
   - 容器高度为卡牌高度的 45%
   - img: width 100%, height 100%, object-fit: cover, object-position: center

3. 场景背景图 (.title-bg img, .campfire-art img, .event-art img):
   - 容器设定固定宽高
   - img: width 100%, height 100%, object-fit: cover

4. 纹理图 (background-image in CSS):
   - background-size: 512px 512px (平铺)
   - 或 background-size: cover (全屏)

5. 装饰元素 (.deco-tape img, .deco-seal img):
   - 容器设定固定宽高
   - img: width 100%, height 100%, object-fit: contain

这样无论原图是 512×512 还是 2048×1536 都不会破版。
```

### 批次 4 验收标准

```
□ 标题页有森林背景图（不管图多大都正常显示）
□ 战斗中玩家和敌人有纸偶图片
□ 篝火场景有篝火图
□ 卡牌有插画（至少 5-10 张常用卡）
□ 所有图片在不同生成尺寸下都不破版
```

---

---

## 批次 5：动效与打磨

### Task 5.1 — 给 Codex 的 prompt（交互动效）

```
为项目添加交互动效，遵循纸艺剪纸风格。

1. 场景过渡动画:
   场景切换时，添加 CSS transition:
   - 旧场景: opacity 1→0, transform scale(1)→scale(0.97), 300ms
   - 新场景: opacity 0→1, transform scale(1.03)→scale(1), 300ms
   （先不做翻页效果，用交叉淡入淡出）

2. 卡牌出牌动画:
   当一张卡从手牌打出时:
   - 卡牌从当前位置飞向目标位置（CSS transition 或 JS animation）
   - 到达目标后: transform scale(0.8) → scale(0) + opacity 0, 200ms
   - 目标位置: 攻击牌→敌人中心坐标, 防御牌→玩家中心坐标

3. 伤害数字飘字:
   创建一个 .damage-number 组件:
   - position absolute, 在受击目标头顶出现
   - font-family var(--font-number), font-size 28px, font-weight bold
   - color var(--color-hp)（伤害红）或 var(--color-heal)（治疗绿）
   - animation: floatUp 0.8s ease-out forwards
   @keyframes floatUp {
     0% { opacity 1; transform translateY(0) scale(1) }
     50% { opacity 1; transform translateY(-30px) scale(1.2) }
     100% { opacity 0; transform translateY(-50px) scale(0.8) }
   }

4. 受击抖动:
   .shake animation:
   @keyframes shake {
     0%, 100% { transform translateX(0) }
     20% { transform translateX(-6px) rotate(-2deg) }
     40% { transform translateX(6px) rotate(2deg) }
     60% { transform translateX(-4px) rotate(-1deg) }
     80% { transform translateX(4px) rotate(1deg) }
   }
   持续 0.3s

5. 奖励卡牌入场:
   .reward-cards .card 依次入场:
   每张 card: opacity 0, translateY(30px) → opacity 1, translateY(0)
   animation-delay: 第1张 0ms, 第2张 150ms, 第3张 300ms
   ease-bounce 缓动

6. 敌人死亡:
   .enemy-slot--dying:
   animation: tearApart 0.5s ease-in forwards
   @keyframes tearApart {
     0% { transform scale(1); opacity 1; filter none }
     50% { transform scale(1.05); filter brightness(2) }
     100% { transform scaleY(0) rotateZ(10deg); opacity 0 }
   }
```

### Task 5.2 — 给 Codex 的 prompt（装饰细节）

```
为项目添加纸艺装饰细节:

1. 缝线分隔线:
   所有使用 border-dashed 的地方，改进为更像缝线的样式:
   border-image: repeating-linear-gradient(
     90deg, 
     var(--ink-secondary) 0px, var(--ink-secondary) 8px, 
     transparent 8px, transparent 12px,
     var(--ink-secondary) 12px, var(--ink-secondary) 13px,
     transparent 13px, transparent 17px
   ) 1;
   （模拟 - - · - - · 的针脚线）
   如果太复杂就保持 dashed 即可。

2. 面板纸胶带装饰:
   .panel 左上角和右上角各添加一个 ::before 和 ::after 伪元素:
   - 宽 40px, 高 16px
   - background: rgba(180,200,160,0.5)（半透明绿色胶带）
   - transform: rotate(-15deg) / rotate(12deg)
   - position absolute, top -6px
   - border-radius: 2px

3. 按钮纸片效果:
   .btn 添加微弱的 background-image:
   使用一个半透明的纸纹 overlay（如果有纹理图的话）
   或用 CSS noise: 
   background-image: url("data:image/svg+xml,...") 极淡的噪点

4. 地图内阴影（旧纸边缘效果）:
   #map-scroll::after {
     content: '';
     position: absolute; inset: 0;
     pointer-events: none;
     box-shadow: inset 0 0 80px 20px rgba(58,46,30,0.25);
   }
```

### 批次 5 验收标准

```
□ 场景切换有淡入淡出过渡
□ 出牌有飞向目标的动画
□ 受到伤害有数字飘字 + 抖动
□ 奖励卡牌依次翻开
□ 敌人死亡有消失动画
□ 面板有纸胶带装饰
□ 地图有旧纸边缘效果
```

---

---

## 给你的 AGENTS.md 模板

> 把这个放到项目根目录，Codex 会自动读取

```markdown
# AGENTS.md — 《锻铸与咒印》Codex 编码规范

## 项目概述
这是一款纸艺剪纸风格的 Roguelike 卡牌游戏，Web 端，1280×720px。

## 美术风格关键词
多层立体剪纸拼贴画、白色裁纸边缘、手工折纸分层感、
牛皮纸纹理、低饱和大地色系、卡通勾线、柔和投影、
奇幻森林童话、治愈童趣、绘本级精细

## CSS 规范
1. 所有颜色使用 tokens.css 中定义的 CSS 变量，禁止硬编码色值
2. 所有间距使用 --space-1 到 --space-7，禁止随意数值
3. 所有阴影使用 --shadow-s/m/l/xl
4. 所有缓动使用 --ease-paper/bounce/smooth
5. 圆角统一 border-radius: 2-4px，不使用大圆角
6. "剪纸"元素必须有: border: 2px solid #fff + box-shadow

## 图片处理
- 所有图片素材尺寸不可预测（由 AI 生成）
- 图片容器必须设定固定宽高
- 角色/物品: object-fit: contain
- 背景/插画: object-fit: cover
- 纹理: background-size: 512px 512px repeat

## 组件复用
- 按钮统一用 .btn 及其变体
- 面板统一用 .panel
- 卡牌统一用 .card
- 新场景优先复用已有组件，避免重复造轮子

## 字体使用
- 标题/按钮: var(--font-display) "ZCOOL KuaiLe"
- 英文标题: var(--font-display-en) "Fredericka the Great"
- 数字: var(--font-number) "Fredoka One"
- 正文: var(--font-body) "Noto Sans SC"
```

---

## 最后给你一张总时间线

| 周 | 做什么 | 核心交付 |
|---|---|---|
| **第 1 周** | 批次 1：Token + 组件 | 全局样式 + 按钮/面板/卡牌组件 |
| **第 2 周** | 批次 2：4 个核心场景 | Title + Battle + Map + Reward 能跑 |
| **第 3 周** | 批次 3：剩余 10 个场景 | 全部 14 场景套上样式 |
| **第 4 周** | 批次 4：出图替换 | AI 生图 + 替换所有占位素材 |
| **第 5 周** | 批次 5：动效打磨 | 过渡动画 + 装饰细节 |
| **第 6 周+** | 持续迭代 | 高级动效、视差、不规则裁边… |

> **每个 Task 的 prompt 都可以直接复制粘贴给 Codex。**
> **每个出图 prompt 都可以直接丢给 Nano Banana。**
> **你的工作就是：复制 prompt → 检查结果 → 下一个。**

有任何某个具体 Task 执行时卡住了，把报错或截图发给我，我帮你调 prompt 👊