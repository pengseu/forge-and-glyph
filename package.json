{
  "name": "fg",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest --run",
    "balance:report": "./node_modules/.bin/vite-node --script scripts/generate-balance-report.ts",
    "assets:check": "./node_modules/.bin/vite-node --script scripts/assets-manifest-sync.ts",
    "assets:sync": "./node_modules/.bin/vite-node --script scripts/assets-manifest-sync.ts --apply"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^7.3.1",
    "vitest": "^3.2.4"
  }
}
